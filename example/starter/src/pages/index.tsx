import { Stack } from "@chakra-ui/react";
import { Card } from "src/ui/card/chain";

const HomePage = () => {
  return (
    <Stack spacing={4}>
      <Card />
    </Stack>
  );
};

export default HomePage;
